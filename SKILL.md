# 安全测试 Skill

> 一套方法论：指导 Agent 在安全测试的**不确定性**中建立**确定性**。
> 灰/黑盒自适应，回归迭代进化。任何能读文件的 Agent 均可驱动。

---

## 一、核心：在不确定性中建立确定性

### 1.1 不确定性从哪来

每个目标系统架构都不同：有无签名/加密网关、什么认证方式、什么响应格式、什么角色体系、什么业务逻辑。**不存在一套固定的用例或字段能覆盖所有项目。** 因此本工具不预设"怎么测"，只提供"如何稳定地测、如何持续地测"。

### 1.2 确定性靠什么建立

确定性不来自"预设字段"，来自**机制**。只要坚持以下 6 个机制，同一目标多次测试就可复现、可对比、可回归：

| # | 机制 | 作用 | 要点 |
|---|------|------|------|
| 1 | **用例历史** | 回归的基础 | 每条用例记录历轮状态（found / fixed / regression / new）与证据引用 |
| 2 | **基线冻结** | diff 的基准 | 每轮结束时冻结本轮「发现清单 + 覆盖清单 + 目标指纹快照」 |
| 3 | **回归 diff** | 判定修没修好、退没退化 | 对比两轮，归类 fixed / still-open / regression / new |
| 4 | **证据规范化** | 可比性 | 证据中易变字段（时间戳/随机数/token）占位化，业务数据原样保留 |
| 5 | **KPI 趋势** | 度量进化 | 每轮记录修复率 / 回归数 / 覆盖率，看曲线 |
| 6 | **红线** | 安全边界 | 禁止 DoS / 破坏数据 / 实际资金 / 短信轰炸 / 大规模暴破 |

> **这 6 个机制是「必须」的。除此之外——字段名、文件格式、用例如何组织、判定如何表达、签名脚本怎么写——全部由 Agent 按目标系统自行设计。** 示例（见 §四）仅供参考，可改、可弃、可自创。

### 1.3 三条原则

1. **机制必须，字段自由** —— 守住 6 个机制，细节任凭发挥。
2. **判定标准随用例走** —— 每条用例自带「预期安全态」的判定标准；只要**同一条用例跨轮用同一标准**，结果就能 diff。标准怎么表达（正则、状态码、意图描述……）由 Agent 定。
3. **示例是参考，不是模板** —— 本目录所有示例文件仅示意一种可行做法，不构成约束。

---

## 二、模式判定（首要步骤）

启动后**第一件事**：扫描 `materials/`，据此决定模式。

### 灰盒（materials/ 非空）
有设计文档。信息源 = 文档 + 线上探测。P0：读文档 → 提取接口/角色/敏感字段 → 线上探测补全 → 交叉验证。

### 黑盒（materials/ 为空）
无文档。信息源 = 线上探测。P0：前端 JS 审计（接口/密钥/签名逻辑）→ 响应头/调试端点探测 → 目录枚举（低速）→ 网关行为分析（**若发现签名/加密层，Agent 自行逆向算法并决定是否需要辅助脚本**）→ 从报错推断参数名。

---

## 三、生命周期

### 3.0 组织维度（多端 / 多角色）

一个被测平台常有多端（小程序 / 移动 App / 桌面端 / Web / 后台管理）。这是**同一项目的内部维度，不拆成多份 kit**——各端共享后端，安全边界在后端，漏洞常跨端（如一处签名缺陷影响所有端）。

组织原则（具体结构由 Agent 按目标架构自行设计，工具不预设）：
- **攻击面按端标注**：接口清单标注所属端（按接口前缀分组或加 scope 字段），覆盖统计才不漏
- **用例按端组织**：端特有接口单独设计用例；端间共享接口只测一次，不重复
- **账户按端配置**：各端认证方式不同（小程序微信 code / 桌面设备凭证 / 后台账密），账户分别准备
- **签名层按端描述**：统一网关一份配置；各端网关不同则分端描述
- **基线/回归按端查看**：发现清单带端维度，回归时既能看跨端整体，也能看按端修复/退化

### 3.1 冷启动 Round 0（从零建基线）

任何新项目都从这里开始，哪怕第一轮零发现。R0 建立「第一份真相」，此后所有迭代以它为回归基准。

1. 判定模式（灰/黑盒）
2. 信息收集（攻击面：接口、角色、认证/授权机制、敏感字段、第三方依赖）
3. 建立用例库：Agent 按攻击面自行设计用例（可参考 `cases/_baseline/` 的维度与示例，可自创、可改写）
4. 逐条执行：Agent 自己决定请求怎么发（需签名时自行解决，可用或不用 `tools/sign.py`），按用例自带的判定标准判 safe/unsafe，冻结证据（易变字段占位化）
5. 冻结 R0 基线：本轮发现清单 + 覆盖清单 + 目标指纹快照
6. 输出 R0 报告，初始化 KPI 趋势

### 3.2 迭代 Round N（回归 + 进化）

项目改版或定期回归时执行。

1. 比对指纹：目标环境漂移则标黄、人工确认（避免把环境变更误判为漏洞变化）
2. 读上轮基线，确定性重放基线用例（含上轮判定为「安全」的正面项）
3. 做 diff，归类四类状态：
   - `fixed` 上轮 open → 本轮 safe（修复成功）
   - `still-open` 上轮 open → 本轮仍 open（待修）
   - `regression` 上轮 safe → 本轮 unsafe（⚠️ 退化警报，比 new 更需警惕）
   - `new` 本轮新发现
4. Agent 分析 diff + 新攻击面，提案新用例
5. 用户确认门：列出拟新增用例与范围，确认后执行
6. 执行新用例 + 复测 still-open 项
7. 冻结 RN 基线 + 追加 KPI 趋势
8. 输出回归报告

### 3.3 归档

项目下线/移交时，整个 `security-test-kit/` 目录（含基线与证据）打包归档供审计。

---

## 四、参考示例（可选，模型自由设计）

> 以下均为**示意**，展示一种可行做法。字段可增删、格式可换、结构可改，只需守住 §1.2 的 6 个机制。

### 4.1 用例示例

见 `cases/000-template.yaml`。核心不是字段，而是**每条用例自带「预期安全态」的判定标准 + 历轮 history**。参考维度（OWASP API Top 10 视角）见 `cases/_baseline/`：

| 维度 | 关注 |
|------|------|
| auth / idor / bfl | 认证、水平越权、垂直越权 |
| ura / conf / recon | 敏感数据暴露、安全配置、信息收集 |
| inject / ssrf / up_xss | 注入、SSRF、上传与存储型 XSS |
| biz / rl / 3rd | 业务逻辑、限流、第三方组件 |

### 4.2 基线示例

每轮冻结到 `output/baseline/`，只需满足"能 diff、能度量"：

- 发现清单（每条含 id、状态、证据引用）
- 覆盖清单（攻击面 → 用例映射，含「测试未发现问题」的正面项）
- 目标指纹快照（响应头/版本/端点清单，用于环境漂移检测）

### 4.3 证据规范化（机制，必须）

证据冻结时，易变字段占位化，否则两轮无法 diff：

| 字段类型 | 处理 |
|----------|------|
| 时间戳类（timestamp/requestId/traceId） | → `<TS>` |
| 随机密钥类（nonce/aes-key） | → `<NONCE>` |
| 会话凭据（token/sessionId） | → `<TOKEN>` |
| 业务数据（code/msg/data） | 原样保留（判定依据） |

### 4.4 签名辅助（可选）

目标有签名/加密网关时，Agent 无法纯文本做密码学运算。`tools/sign.py` 是一份**参考实现**（读 `config/signer.yaml` 描述，做签名/加密/解密）。Agent 可：

- 直接用（配 `config/signer.yaml`，见其 `.example`）
- 改写扩展，适配非常规算法
- **完全弃用，自行写脚本**（放 `tools/` 下）

签名机制本身是"发现"出来的：黑盒 P0 探测发现签名头 → Agent 逆向算法 → 决定用/改/弃 sign.py。无签名项目零依赖。

---

## 五、红线（禁止）

| 禁止 | 允许 |
|------|------|
| DoS / 高并发 | 低速率单次探测（默认 1 req / 5s） |
| 破坏真实数据（DELETE/DROP/TRUNCATE） | 只读探查 |
| 实际资金操作 | 0.01 元逻辑验证 |
| 高频短信/邮件轰炸 | 使用测试账户 |
| 大规模字典暴破 | 签名/加密逆向（公开前端 JS 范围内） |
| 修改他人账户 | 写操作须确认 |

**执行原则**：高危发现立即通报；不确定的写操作先确认；限速由 Agent 遵守 + 用户监督。

---

## 六、目录结构

```
security-test-kit/
├── SKILL.md                  # 本文件 - 方法论 + 机制 + 生命周期
├── cases/
│   ├── 000-template.yaml     #   用例示例（参考，非模板）
│   ├── _baseline/            #   参考维度用例（示意，可自建/改写）
│   └── <project>/            #   项目专属用例（Agent 自行设计，不提交）
├── config/                   # 项目配置（.example 为参考模板）
│   ├── signer.yaml.example
│   ├── accounts.yaml.example
│   └── target.yaml.example
├── tools/sign.py             #   签名辅助（参考实现，可弃用）
├── templates/                # 报告模板（参考）
├── materials/                # 设计文档（按项目放）
└── output/                   # 测试产出（基线/证据/报告，不提交）
```

---

## 七、复用指南

1. 克隆本工具包到目标项目目录
2. 放设计文档到 `materials/`（可选，决定灰/黑盒）
3. 参考 `config/*.yaml.example` 配置目标与账户（可选）
4. 对 Agent 说："使用 security-test-kit 对 <目标> 进行安全测试，目标地址：https://..."

Agent 读 SKILL.md 后自行完成模式判定、用例设计、执行、建基线、回归。**工具只给机制和参考，怎么测由 Agent 按目标决定。**

---

_本工具约定的是机制，不是字段。确定性来自 6 个机制，自由度留给驱动 Agent。_
