# 安全测试 Skill

> 通用 Web/API 安全测试框架。灰盒/黑盒双模式，有资料优先看资料，没有也能测。

---

## 一、Skill 概述

本 Skill 引导完成一次结构化的安全测试，从模式判定、信息收集、方案制定到执行和报告输出。

**核心设计原则**：
- **自适应模式**：先检查 `materials/` 目录，有文档走灰盒，没有走黑盒
- **先计划后执行**：出方案 → 确认 → 执行 → 出报告
- **红线明确**：禁止 DoS、禁止破坏真实数据、禁止实际资金操作

---

## 二、模式判定（首要步骤）

启动测试后的**第一件事**是扫描 `materials/` 目录，据此决定测试模式：

### 灰盒模式（materials/ 非空）

有设计文档时使用。信息源包括文档 + 线上探测。

| 优势 | 劣势 |
|------|------|
| 接口清单、参数定义直接来自文档 | 可能遗漏文档未覆盖的接口 |
| 身份角色体系明确 | 文档可能与实际实现不一致 |
| 测试用例可以精确到字段名 | 需要交叉验证文档准确性 |

**P0 步骤**：① 读取 materials/ 所有文档 → ② 提取接口清单 → ③ 线上探测补充暴露面 → ④ 交叉验证文档与实际情况

### 黑盒模式（materials/ 为空）

没有任何设计文档时使用。信息源仅包括线上探测。

| 优势 | 劣势 |
|------|------|
| 完全模拟外部攻击者视角 | 接口发现依赖逆向和猜测 |
| 不依赖文档准确性 | 可能遗漏隐藏接口 |
| 测试结果更贴近真实威胁 | 参数名需从报错或 JS 推断 |

**P0 步骤**：① 前端 JS 审计（提取接口、密钥、签名逻辑）→ ② 响应头/调试端点探测 → ③ 目录/文件枚举（低速 ffuf）→ ④ 网关行为分析（有签名层则逆向）→ ⑤ 从报错信息推断参数名

---

## 三、目录结构

```
security-test-kit/
├── README.md                 # 快速开始
├── SKILL.md                  # 本文件 - Skill 定义 + AI 行为指令
├── materials/                # 设计文档（可为空）
│   └── README.md             # 资料清单说明
├── templates/                # 报告模板
│   ├── test-plan.md          # 测试方案模板
│   ├── test-report.md        # 测试报告模板
│   └── executive-summary.md  # 一页摘要模板
└── output/                   # 测试产出
```

---

## 四、使用方式

### 4.1 准备

1. 将本目录复制到目标项目（或独立使用）
2. 将设计文档放入 `materials/`（可选；没有就空着）
3. 确保已取得**书面测试授权**

### 4.2 启动

```
使用 security-test-kit 对 <目标系统> 进行安全测试。
目标地址：https://example.com
测试范围：Web / API / 小程序 / 后台管理
```

### 4.3 执行流程

| 阶段 | 灰盒 | 黑盒 |
|------|------|------|
| **P0 信息收集** | 读 materials/ + 线上探测 | 前端 JS 逆向 + 端口扫描 + fuzz |
| **P1 制定方案** | 基于文档接口清单编写用例 | 基于发现的接口编写用例 |
| **P2 执行测试** | 逐用例验证，对照文档预期 | 逐用例验证，推断预期行为 |
| **P3 输出报告** | 报告注明"灰盒"及信息来源 | 报告注明"黑盒"及探测手段 |

---

## 五、AI 行为指令

### P0. 信息收集

**首要动作：判定模式**

```
1. 列出 materials/ 目录内容
2. 如果有文件 → 灰盒模式，读取所有文档
3. 如果为空 → 黑盒模式，跳过文档读取
4. 将模式告知用户，确认后继续
```

**灰盒路径 — Step 0.1：读取文档**

| 文件类型 | 处理方式 |
|----------|----------|
| `.md` | 直接读取 |
| `.pdf` | 调用 PDF 读取工具；>50 页建议 `--pages` 限制 |
| `.docx` | 调用 Word 读取工具 |
| `.pptx` | 调用 PPT 读取工具 |
| `.wps` | 提示用户另存为 .docx |

从文档中提取：
- 接口清单（URL、方法、参数、鉴权要求）
- 身份角色体系（用户类型、权限层级）
- 认证/授权机制（token 格式、签名算法）
- 敏感数据字段（密码、手机号、密钥等）
- 第三方依赖（大模型 API、支付、短信等）

**黑盒路径 — Step 0.1：外部探测**

从零开始摸清攻击面：
- 下载前端 JS，从中提取：接口路径、硬编码密钥/AppId、签名算法、隐藏路由
- 探测常见调试端点（Swagger、Actuator、Druid、/health、/.git、*.bak 等）
- 响应头分析（Server 指纹、安全头缺失、CORS 配置）
- 网关/鉴权层行为探测（未认证请求的响应差异、签名校验逻辑）
- 如有签名/加密层：从前端 JS 逆向算法，编写客户端复刻

**共用 — Step 0.2：攻击面判定**

无论哪种模式，最终输出：
- 已鉴权接口列表
- 未鉴权接口列表
- 跨角色接口列表
- 高危目标（支付、退款、用户数据、第三方 Key 等）
- 认证机制说明（token 格式、签名公式、加密方式）

### P1. 制定方案

按以下维度生成测试用例，输出到 `output/<项目>-测试方案.md`：

| 类别 | 测试项 |
|------|--------|
| 认证 | 弱口令、暴破无防护、token 伪造/过期、会话固定、登出失效 |
| 授权 | 水平越权(IDOR)、垂直越权、跨租户访问 |
| 敏感数据 | 响应泄露密码/手机号/密钥、未授权接口返回敏感数据 |
| 注入 | SQL 注入、命令注入、SSTI、XXE |
| 批量赋值 | 客户端可写敏感字段（payStatus、userId、role 等） |
| 业务逻辑 | 支付绕过、退款篡改、金额客户端可控、权益超用 |
| 安全配置 | CORS、安全头、调试端点暴露、默认口令、路径未入网关 |
| 资源限制 | 无验证码、无 IP 限速、枚举攻击、账户 DoS |
| 第三方 | API Key 泄露、回调签名缺失、组件已知漏洞 |

**方案须经用户确认后再进入执行阶段。**

### P2. 执行测试

**红线**（禁止）：
- DoS 攻击、高并发、死循环
- 破坏真实数据（DELETE、DROP、TRUNCATE）
- 实际资金转移
- 高频短信/邮件轰炸
- 大规模字典暴破（须额外授权）
- 修改他人账户密码/绑定

**允许**：
- 只读数据探查
- 低速率单次请求探测
- 使用测试账户
- 逻辑验证（传无效参数、越权探针）
- 签名/加密算法逆向（公开前端 JS 范围内）
- 0.01 元测试支付（仅验证逻辑）

**执行原则**：
- 每个用例记录：接口、输入、预期、实际结果、风险等级
- 高危发现立即通报
- 不确定的操作先确认

### P3. 输出报告

按 `templates/test-report.md` 和 `templates/executive-summary.md` 模板输出，保存到 `output/`。

报告要求：
- 区分"已坐实的问题"和"测试了未发现问题的项"
- 每个问题含：描述、复现步骤、影响、修复建议
- 风险定级：严重 / 高 / 中 / 低
- 一页摘要面向管理层，不含技术细节
- 报告标题注明测试性质（灰盒/黑盒）

---

## 六、复用指南

### 为新项目配置

```bash
# 1. 复制工具包
cp -r security-test-kit /path/to/new-project/

# 2. 放入资料（可选）
cp api-docs.md /path/to/new-project/security-test-kit/materials/

# 3. 清理上次产出
rm /path/to/new-project/security-test-kit/output/*

# 4. 启动测试
```

### 产出归档

测试完成后，整个 `security-test-kit/` 目录（含 `output/` 中的报告）可打包归档供审计。
